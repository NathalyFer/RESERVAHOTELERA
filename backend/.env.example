# Configuración de Base de Datos Oracle Cloud

# Opción 1: Usando Connection String (Recomendado para Oracle Cloud)
# DB_DSN=(description= (retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.us-ashburn-1.oraclecloud.com))(connect_data=(service_name=tu_servicio_high.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))

# Opción 2: Usando Wallet (Más seguro)
# Descarga el wallet desde Oracle Cloud Console
# Descomprime el wallet en una carpeta (ej: C:\oracle\wallet)
# DB_DSN=tu_servicio_high
# Además necesitas configurar TNS_ADMIN en el código o como variable de entorno

# Configuración básica (edita estos valores)
DB_USER=ADMIN
DB_PASS=tu_password_oracle_cloud
DB_DSN=localhost:1521/XEPDB1

# Para Oracle Cloud Autonomous Database:
# - Usuario por defecto: ADMIN
# - Puerto: 1522 (con TLS) o 1521 (sin TLS)
# - Host: Obtén desde Oracle Cloud Console → Database Connection
# - Service Name: Obtén desde Oracle Cloud Console (termina en _high, _medium, o _low)
